[pytest]
# 只收集主测试目录：
#   - gmail_android/tests 依赖 appium（可选依赖，默认环境未装），且其 config.py
#     会在收集时遮蔽根 config.py，导致大量 ImportError —— 不纳入默认收集。
#   - engine/* 是独立应用，各自带 pytest.ini，单独在其目录内运行。
testpaths = tests
norecursedirs = .* build dist *.egg engine gmail_android vendor node_modules
